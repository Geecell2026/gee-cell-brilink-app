# Salin ke /etc/nginx/sites-available/gee-cell-brilink-app
# lalu: sudo ln -s /etc/nginx/sites-available/gee-cell-brilink-app /etc/nginx/sites-enabled/
# Ganti "domain-anda.com" dengan domain/subdomain Anda, lalu jalankan:
#   sudo nginx -t && sudo systemctl reload nginx
#   sudo certbot --nginx -d domain-anda.com

server {
    listen 80;
    server_name domain-anda.com;

    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}
